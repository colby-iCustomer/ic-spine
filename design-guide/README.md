# design-guide/

Visual + interaction standards for iCustomer's surfaces — design tokens, component patterns, voice + copy.

## Status

🌡️ **Placement is an open question.** Currently lives at the root. Alternatives considered:

- Inside `architecture/` (alongside platform structure)
- Inside `handbooks/engineering/` (treated as eng-side concern)
- Per-product (a separate design-guide per product tier)

Decision to be made at team meeting. Current default: root.

## Planned content

- `tokens.md` — design tokens (color, type, spacing)
- `components.md` — component patterns (buttons, drawers, modals, etc.)
- `voice-and-copy.md` — brand voice + copy conventions
- `motion.md` — animation + transition guidelines

## Source

Draft design-system content exists in `ic-product-mgmt/projects/aloop-v3-product-design/design-system/`. To be ported once the placement question is resolved.
